# 2026-09-05

## DbMigrator build fix (3 files)

User hit 9 build errors. Actual root cause: missing `using` directives cascading across projects.

**Fixes applied:**
- `Shared/ERPPlatform.Domain/Imports/IEmployeeImportJobScheduler.cs` — added `using System;` (lines 27/29 `Guid` errors).
- `Shared/ERPPlatform.EntityFrameworkCore/EntityFrameworkCore/ERPPlatformDbContext.cs`:
  - Added `using Volo.Abp.EntityFrameworkCore.Modeling;` (resolves `ConfigureByConvention` for EmployeeImport Job/Chunk/Error).
  - Added `using ERPPlatform.Imports;` (resolves `EmployeeImportConsts` — lives in `ERPPlatform.Imports` namespace, NOT `ERPPlatform.Domain.Imports`).

**Insight:** `dotnet build` reports errors project-by-project in dependency order. When `ERPPlatform.Domain` failed first (Guid), the EntityFrameworkCore errors were hidden. Fixing Domain surfaced the next 7 errors. **Always rebuild after each fix** when multiple projects share errors — the screenshot showed 9 errors total, but each `build` only showed the current failing project.

## Backend / DbMigrator blockers (still pending)

- Backend (`ERPPlatform.HttpApi.Host`): cannot bind to 44327 — **IIS Express (PID 62440) squatting the port**. Fix: kill iisexpress, then `cd Host/ERPPlatform.HttpApi.Host && dotnet run`.
- DbMigrator: build is now green, but `db66804.databaseasp.net` resolves to private IP `10.0.0.31:1433` (unreachable from this network). LocalDB alternative is commented out in `appsettings.json`. User must either VPN into the LAN or repoint to LocalDB.
