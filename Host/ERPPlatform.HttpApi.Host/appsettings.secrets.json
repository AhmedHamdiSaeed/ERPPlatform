{
  // NOTE: no "ConnectionStrings" section on purpose.
  // ABP loads appsettings.secrets.json LAST, so it overrides BOTH appsettings.Production.json
  // and environment variables. Leaving the connection string here pinned every environment -
  // including the deployed site - to LocalDB, which does not exist on shared hosting (500 on
  // every URL). Dev now falls back to appsettings.json (LocalDB); Production picks up
  // appsettings.Production.json (remote SQL Server).
  "StringEncryption": {
    "DefaultPassPhrase": "wy7tFY7ddGGwv5t8"
  },
  "Auth": {
    "JwtSecret": "ERPPlatformSecretKeyForJwtSigningMustBe32CharsLong!"
  }
}